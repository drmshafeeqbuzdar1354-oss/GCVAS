export default function Home() {
  return (
    <main style={{fontFamily: 'system-ui, -apple-system, Segoe UI, Roboto, sans-serif', padding: '2rem'}}>
      <h1>Welcome to GCVAS</h1>
      <p>This is a minimal Next.js scaffold added so Vercel can detect and build the app.</p>
      <p>Keep your existing Node scripts; they remain available via npm run ai or npm run ai-gateway.</p>
    </main>
  );
}
